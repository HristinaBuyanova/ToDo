# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ToDo' do
 
  use_frameworks!
  pod 'SwiftLint'

  target 'ToDoTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
